language: java
jdk:
  - openjdk11
  - gem install faraday -v 1.9.3

sudo: required

before_install:
 - chmod +x gradlew
 
script:
 - ./gradlew build
  
deploy:
  provider: heroku
  api_key: $HEROKU_TOKEN
  on: main
  app: "bookstore-service"